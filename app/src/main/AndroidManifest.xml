<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.samblog.easymvp.sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="cn.samblog.easymvp.sample.ui.activity.loading.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.index.MainActivity" />

        <activity android:name=".ui.activity.user.UserActivity" />

        <activity android:name=".ui.activity.custom.CustomActivity"/>

        <activity android:name=".ui.activity.other.RecyclerViewAdapterDemo1Activity" />
        <activity android:name=".ui.activity.other.SimpleFragmentActivity"/>

    </application>

</manifest>